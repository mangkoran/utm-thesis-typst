#import "@preview/tablex:0.0.7": tablex, colspanx

*Declaration of Cooperation*

This is to confirm that this research has been conducted through a collaboration
Alice and Bob.

// #v(6em)
#align(horizon,
  [
    #tablex(
      auto-lines: false,
      columns: (auto, 1fr),
      [Certified by:], [],
      colspanx(2)[], (),
      colspanx(2)[], (),
      colspanx(2)[], (),
      [Signature:], [],
      colspanx(2)[], (),
      colspanx(2)[], (),
      colspanx(2)[], (),
      [Name:], [],
      [Position:], [],
      [Official Stamp:], [],
      colspanx(2)[], (),
      colspanx(2)[], (),
      colspanx(2)[], (),
      colspanx(2)[Date: #datetime.today().display()], (),
      colspanx(2)[], (),
      colspanx(2)[], (),
      colspanx(2)[], (),
    )
  ]
)

#align(bottom,
  [
    \*This section is to be filled up for theses with industrial collaboration
  ]
)
